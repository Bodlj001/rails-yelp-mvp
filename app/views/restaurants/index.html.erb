<h1>All Restaurants</h1>

<%= link_to "Add restaurant", new_restaurant_path %>

<% @restaurants.each do |rest| %>
  <h3><%= link_to rest.name, restaurant_path(rest.id) %></h3>
  <h5><%= rest.category %></h5>
  <p><%= rest.phone_number %></p>
<% end %>
